import { useContext } from "react";
import Layout from "../../Components/Layout";


const Home = () => {
    return(
        <Layout>
            <h1>home</h1>    
        </Layout>
    );
};

export default Home;